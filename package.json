{
  "name": "market-suggestion-extension",
  "version": "1.0.0",
  "description": "Chrome extension that suggests relevant markets on Kalshi based on webpage content with real-time API integration",
  "main": "background.js",
  "scripts": {
    "build": "npm run build:config && npm run build:css && cp node_modules/node-forge/dist/forge.min.js lib/forge.min.js",
    "build:config": "node scripts/build-config.js",
    "build:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify",
    "watch:css": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "dev": "npm run build:config",
    "proxy:setup": "node scripts/deploy-proxy.js",
    "proxy:start": "cd server && npm start",
    "proxy:dev": "cd server && npm run dev",
    "heroku:deploy": "node scripts/deploy-heroku.js",
    "test:proxy": "node scripts/test-proxy.js",
    "test": "echo 'Add tests here'",
    "lint": "echo 'Add linting here'",
    "install-extension": "echo 'Load the extension in Chrome by going to chrome://extensions/ and enabling Developer mode, then click Load unpacked and select this directory'"
  },
  "keywords": [
    "chrome-extension",
    "prediction-markets",
    "kalshi",
    "polymarket",
    "content-analysis",
    "market-suggestions",
    "api-integration"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "daisyui": "^4.12.24",
    "dotenv": "^16.3.1",
    "tailwindcss": "^3.4.19"
  },
  "dependencies": {
    "node-forge": "^1.3.1"
  }
}
